<h1>Click On Your Name to Checkout Books</h1>
<hr />
@if (Model.Count != 0)
{
  <ul>
    @foreach (var member in Model)
    {
      <li><a href='/checkout/@member.GetId()'>@member.GetMember()</a></li>
    }
  </ul>
}
